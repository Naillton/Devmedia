import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  headContainer: {
    width: '100%',
    backgroundColor: 'black',
    height: 60,
    marginTop: 15,
  },

  title: {
    fontSize: 30,
    color: '#00ffff',
    textAlign: 'center',
    padding: 10,
  },
});
