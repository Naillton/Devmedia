import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  containerImg: {
    flex: 1,
    alignItems: "center",
    justifyContent: "center",
  },

  container: {
    textAlign: "center",
    width: 300,
    height: 400,
  },

  title: {
    fontSize: 26,
    fontWeight: "bold",
    textAlign: "center"
  },

  text: {
    fontSize: 16,
  }
})