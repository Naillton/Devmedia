import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  contContainer: {
    alignItems: 'center',
    justifyContent: 'center'
  },

  contTitle: {
    fontSize: 30,
    fontWeight: 'bold',
    marginVertical: 20,
  },

  contImg: {
    width: 250,
    height: 250,
  },

  contText: {
    fontSize: 18,
    marginVertical: 10,
  }
});