import React from "react";
import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  topoContainer: {
    alignItems: 'center',
    backgroundColor: 'red',
    padding: 20,
  },

  topoImg: {
    width: 200,
    height: 200,
  },

  topoText: {
    color: '#fff',
    fontSize: 20,
    fontWeight: 'bold',
  }
});