import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: '#B5F757'
  },

  text: {
    textAlign: 'center',
    fontSize: 20,
  },

  image: {
    width: 300,
    height: 300,
    marginBottom: 20,
  }
});
