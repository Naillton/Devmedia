import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    alignItems: "center",
  },

  img: {
    width: 500,
    height: 200,
  },

  imgPerf: {
    margin: 10,
    width: 200,
    height: 200,
    borderRadius: 100,
  }
});